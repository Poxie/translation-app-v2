import { View } from 'react-native';
import { VocItem } from "../../types";

export const EditCategory: React.FC<{
    defaultItem?: VocItem;
}> = ({ defaultItem }) => {
    return(
        <View>
            
        </View>
    )
}